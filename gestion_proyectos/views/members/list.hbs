<section class="content-header" style="position: relative;">
  <h1 class="flex-horizontal-start">
    <b style="margin-right:2%;">Lista de Integrantes</b>
    <select class="form-control" name="project" required style="width: 20%;">
        <option value="-1">Todos</option>
        {{#each projects}}
        <option value="{{id}}" {{#if select}}selected{{/if}}>{{name}}</option>
        {{/each}}
      </select>
  </h1>
  <ol class="breadcrumb">
    <li><a href="#"><i class="fa fa-tasks"></i>Integrantes</a></li>
    <li class="active">Listar</li>
  </ol>
</section>
<hr>
<section class="content">
  <div class="row">
    <div class="col-md-12">
      <label>{{error}}</label>
    </div>
  </div>
  <div class="row flex-centered">
    <div class="col-md-10">
      <table class="table table-striped table-condensed table-hover table-bordered">
        <thead>
          <tr>
            <th>Documento</th>
            <th>Integrante</th>
            <th>Projecto</th>
            <th style="width: 120px;">Acciones</th>
          </tr>
        </thead>
        <tbody>
          {{#each members}}
            <tr>
              <td>{{document}}</td>
              <td>{{user}}</td>
              <td>{{project}}</td>
              <td style="width: 120px;">
                <ul class="list-buttons">
                  <li>
                    <a data-toggle="modal" data-target="#deleteConfirm{{id}}" class="btn btn-danger btn-block">
                      <i class="fa fa-trash" aria-hidden="true"></i>
                    </a>
                  </li>
                </ul>
              </td>
            </tr>
            <div id="deleteConfirm{{id}}" class="modal fade" role="dialog">
              <div class="modal-dialog">
                <!-- Modal content-->
                <div class="modal-content">
                  <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal">&times;</button>
                    <h4 class="modal-title">Eliminar Integrante</h4>
                  </div>
                  <div class="modal-body">
                    <p>¿Desea eliminar el integrante {{user}}?</p>
                  </div>
                  <div class="modal-footer">
                    <div class="row">
                      <div class="col-md-6">

                      </div>
                      <div class="col-md-3">
                        <button type="button" class="btn btn-block btn-flat btn-info" data-dismiss="modal">Cancelar</button>
                      </div>
                      <div class="col-md-3">
                        <a type="button" class="btn btn-block btn-flat btn-danger" href="/member/delete/{{id}}">Eliminar</a>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          {{/each}}
        </tbody>
      </table>
    </div>
  </div>
</section>
<script type="text/javascript">
  $(function() {
    // bind change event to select
    $('.float-select').on('change', function() {
      var url = '/member/list/' + $('.float-select').val(); // get selected value
      if (url) { // require a URL
        window.location = url; // redirect
      }
      return false;
    });
  });
</script>
